Opcional: coloca aquí un binario FFmpeg administrado por el proyecto.

Windows:
  tools/ffmpeg/bin/ffmpeg.exe

Linux/macOS:
  tools/ffmpeg/bin/ffmpeg

El proyecto intentará este binario antes de usar el FFmpeg incluido por
la dependencia imageio-ffmpeg o el PATH del sistema.

# Solo si necesitas conservar rclone como proveedor opcional.
# El contenido real depende de tu cuenta y proveedor y NO debe versionarse.
[remote_drive]
type = drive
scope = drive
# Configura este remoto con `rclone config` en tu máquina.

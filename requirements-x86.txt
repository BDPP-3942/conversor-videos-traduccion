# Dependencias específicas del build Windows x86 (32 bits).
# Se instalan separadamente porque PEP 508 no permite distinguir de forma
# fiable un intérprete Python de 32 bits bajo Windows x64.
vosk==0.3.42
imageio-ffmpeg==0.6.0
webvtt-py==0.5.1
python-dotenv==1.2.3
pyinstaller==6.22.2
